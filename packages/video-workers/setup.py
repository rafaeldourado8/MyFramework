from setuptools import setup, find_packages

setup(
    name="video-workers",
    version="1.0.0",
    description="Background workers for video processing",
    packages=find_packages(),
    python_requires=">=3.11",
)
