from setuptools import setup, find_packages

setup(
    name="video-processing",
    version="1.0.0",
    description="Video processing with thumbnails and clips",
    packages=find_packages(),
    python_requires=">=3.11",
)
