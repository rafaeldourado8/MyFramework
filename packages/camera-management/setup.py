from setuptools import setup, find_packages

setup(
    name="camera-management",
    version="1.0.0",
    description="Camera management with RTSP support",
    packages=find_packages(),
    python_requires=">=3.11",
)
